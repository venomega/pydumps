import json
import storage # for global vars support
p_nick="./nick.json"
d = {}
try:
    open(p_nick,"r").read()
except:
    open(p_nick,"w").write("")

def _save_nick(nick,addr):
    json.load(storage.d,storage.)
    storage.d[nick] = address
def _load_nick(path):
    
def put_nick(nick,address):
    _save(nick,address)

    json.dump(d,storage.p_nick)
